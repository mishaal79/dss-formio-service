# DSS Form.io Service - Environment Variables
# Copy this file to .env and update with your values
# 
# SECURITY NOTE: The .env file is gitignored for security
# Only these minimal variables are required - all passwords are auto-generated securely

# Form.io Configuration (Required)
export TF_VAR_formio_license_key="your-enterprise-license-key-here"
export TF_VAR_formio_root_email="admin@your-domain.com"

# Optional Overrides (uncomment if needed)
# export TF_VAR_project_id="erlich-dev"  # Defaults to erlich-dev, override for other projects

# Usage:
# 1. Copy this file: cp .env.example .env  
# 2. Update .env with your license key and email
# 3. Load before terraform: source .env
# 4. Run terraform: terraform plan
#
# All other secrets (passwords, JWT secrets, etc.) are automatically generated
# using cryptographically secure methods and stored in Google Secret Manager